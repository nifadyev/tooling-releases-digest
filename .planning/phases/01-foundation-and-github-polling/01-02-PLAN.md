---
phase: 01-foundation-and-github-polling
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/github.py
  - src/main.py
  - tests/test_github.py
  - tests/test_cli.py
autonomous: true
requirements:
  - CORE-01
must_haves:
  truths:
    - Application polls GitHub API for configured projects
    - New releases are saved to the database
    - The CLI execution loops through all configured projects
  artifacts:
    - path: src/github.py
      provides: GitHub API fetching using httpx
      exports: [fetch_latest_release]
    - path: src/main.py
      provides: CLI entrypoint
  key_links:
    - from: src/main.py
      to: src/github.py
      via: fetch_latest_release call
      pattern: fetch_latest_release
    - from: src/main.py
      to: src/db.py
      via: DB operations
      pattern: save_release
---

<objective>
Implement the GitHub API polling logic and the main CLI application loop.

Purpose: Provides the core logic to query GitHub and coordinate between the config, DB, and network.
Output: GitHub HTTP client module and main CLI script.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-github-polling/01-CONTEXT.md
@.planning/phases/01-foundation-and-github-polling/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GitHub Polling Engine</name>
  <files>src/github.py, tests/test_github.py</files>
  <read_first>
    - src/github.py
  </read_first>
  <behavior>
    - Test 1: Successful response returns a dict with tag_name and published_at
    - Test 2: Rate limit response raises an explicit exception
    - Test 3: HTTP error logs properly and propagates
  </behavior>
  <action>
    - Implement `src/github.py`. Use `httpx.Client`.
    - Create `fetch_latest_release(client: httpx.Client, repo: str) -> dict`. It should GET `https://api.github.com/repos/{repo}/releases/latest`.
    - Pass headers `Authorization: Bearer <token>` and `X-GitHub-Api-Version: 2022-11-28`.
    - Add error handling: raise custom exception `GitHubRateLimitError` on HTTP 403 / rate limits, and raise for status for others.
    - Setup tests using `respx.mock` to mock `https://api.github.com`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_github.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/github.py contains httpx.Client`
    - `tests/test_github.py contains respx.mock`
    - `uv run pytest tests/test_github.py exits 0`
  </acceptance_criteria>
  <done>Can fetch latest release data securely from GitHub API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI Integration</name>
  <files>src/main.py, tests/test_cli.py</files>
  <read_first>
    - src/main.py
  </read_first>
  <behavior>
    - Test 1: CLI loads config, initializes DB, processes multiple projects, saves new releases, and skips existing
  </behavior>
  <action>
    - Implement `main()` in `src/main.py`.
    - Load settings via `src.config.Settings()`.
    - Set up logging via `setup_logging()`.
    - Load `projects.toml` from settings.
    - Initialize database via `init_db`.
    - Use `httpx.Client` with the token. Iterate through each project in the config.
    - For each project, fetch latest release. Use structlog to `logger.info("checking_project", repo=project["repo"])`.
    - Check if processed via `is_release_processed`. If not, `save_release` and log `logger.info("new_release_found", repo=..., tag=...)`.
    - If error fetching a project, catch it, log `logger.error("project_fetch_failed")` and continue to the next.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/main.py contains if __name__ == "__main__":`
    - `src/main.py contains logger.info("checking_project"`
    - `src/main.py contains except Exception as e:` wrapping the project processing loop iteration
  </acceptance_criteria>
  <done>Main execution loops through projects robustly.</done>
</task>

</tasks>

<verification>
`uv run pytest tests/` passes successfully.
</verification>

<success_criteria>
- Core GitHub polling works with authentication
- New releases are successfully detected and persisted
- Main application runs sequentially over all projects
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-github-polling/01-02-SUMMARY.md`
</output>