---
phase: 01-foundation-and-github-polling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .env.example
  - src/config.py
  - src/db.py
  - tests/test_config.py
  - tests/test_db.py
  - tests/conftest.py
autonomous: true
requirements:
  - CORE-02
  - CORE-03
must_haves:
  truths:
    - Application configuration can be loaded from env and TOML
    - Database schema can be initialized
    - State can be queried and persisted in SQLite
  artifacts:
    - path: src/config.py
      provides: Settings and TOML parsing
      min_lines: 20
    - path: src/db.py
      provides: SQLite connection and operations
      exports: [init_db, save_release, is_release_processed]
  key_links:
    - from: src/config.py
      to: pydantic_settings
      via: Settings class
      pattern: BaseSettings
---

<objective>
Establish the project foundation including dependency management, configuration loading, structured logging setup, and the SQLite state database schema.

Purpose: Provides the 12-factor foundation and persistence layer required by the GitHub polling engine.
Output: Project layout, configuration parser, and database wrapper.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-github-polling/01-CONTEXT.md
@.planning/phases/01-foundation-and-github-polling/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project Foundation and Configuration</name>
  <files>pyproject.toml, .env.example, src/config.py, tests/test_config.py, tests/conftest.py</files>
  <read_first>
    - .planning/phases/01-foundation-and-github-polling/01-RESEARCH.md
    - src/config.py
  </read_first>
  <behavior>
    - Test 1: Given a mock projects.toml, Settings loads the projects correctly
    - Test 2: Given missing GITHUB_TOKEN, Settings validation fails
  </behavior>
  <action>
    - Update `pyproject.toml` using `uv add pydantic-settings structlog httpx` and `uv add --dev pytest pytest-xdist faker respx mypy ruff`.
    - Create `src/config.py` with `pydantic_settings.BaseSettings`. Include fields `github_token: str`, `projects_file: Path = Path("projects.toml")`, `db_path: Path = Path("state.db")`, `log_level: str = "INFO"`.
    - Create a function `load_projects(path: Path) -> list[dict]` in `src/config.py` that reads the TOML using `tomllib.load(f)`.
    - Create `.env.example` with `GITHUB_TOKEN=your_pat_here`.
    - Set up structlog in `src/config.py` via a `setup_logging()` function that configures `structlog.configure()` for JSON stdout output.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/config.py contains class Settings(BaseSettings):`
    - `src/config.py contains setup_logging()`
    - `uv run ruff check src/config.py exits 0`
  </acceptance_criteria>
  <done>Configuration loads successfully and logging is initialized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: State Storage SQLite Schema</name>
  <files>src/db.py, tests/test_db.py</files>
  <read_first>
    - src/db.py
  </read_first>
  <behavior>
    - Test 1: init_db creates the releases table
    - Test 2: save_release successfully inserts a record
    - Test 3: is_release_processed returns True for existing, False for new
  </behavior>
  <action>
    - Implement `src/db.py`. Do not use SQLAlchemy. Use raw `sqlite3`.
    - Create `init_db(db_path: Path)` which runs: `CREATE TABLE IF NOT EXISTS releases (repo TEXT, tag TEXT, published_at TEXT, discovered_at TEXT, PRIMARY KEY (repo, tag))`
    - Create `save_release(db_path: Path, repo: str, tag: str, published_at: str)` that inserts a record (with current UTC ISO time for `discovered_at`).
    - Create `is_release_processed(db_path: Path, repo: str, tag: str) -> bool` to check if a release already exists.
  </action>
  <verify>
    <automated>uv run pytest tests/test_db.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/db.py contains CREATE TABLE IF NOT EXISTS releases`
    - `uv run mypy src/db.py exits 0`
  </acceptance_criteria>
  <done>Database logic allows tracking releases accurately.</done>
</task>

</tasks>

<verification>
`uv run pytest tests/test_config.py tests/test_db.py` passes successfully.
</verification>

<success_criteria>
- Configuration and logging foundation established
- Local database operations built and tested
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-github-polling/01-01-SUMMARY.md`
</output>