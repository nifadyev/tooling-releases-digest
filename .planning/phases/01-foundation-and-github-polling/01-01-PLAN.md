---
phase: 01-foundation-and-github-polling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .env.example
  - src/config.py
  - src/db.py
  - tests/test_config.py
  - tests/test_db.py
  - tests/conftest.py
autonomous: true
requirements:
  - CORE-02
  - CORE-03
must_haves:
  truths:
    - Application configuration can be loaded from env and TOML
    - Database schema can be initialized
    - State can be queried and persisted in SQLite
  artifacts:
    - path: src/config.py
      provides: Settings and TOML parsing
      min_lines: 20
    - path: src/db.py
      provides: SQLite connection and operations
      exports: [init_db, save_release, is_release_processed]
  key_links:
    - from: src/config.py
      to: pydantic_settings
      via: Settings class
      pattern: BaseSettings
---

<objective>
Establish the project foundation including dependency management, configuration loading, structured logging setup, and the SQLite state database schema.

Purpose: Provides the 12-factor foundation and persistence layer required by the GitHub polling engine.
Output: Project layout, configuration parser, and database wrapper.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-github-polling/01-CONTEXT.md
@.planning/phases/01-foundation-and-github-polling/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Project Initialization</name>
  <files>pyproject.toml, lefthook.yml, .gitignore</files>
  <action>
    - Run `uv init --lib`.
    - Create virtual environment: `uv venv ~/.virtualenvs/tooling-releases-digest`.
    - Add development dependencies: `uv add --dev lefthook ruff mypy flake8 wemake-python-styleguide flake8-pyproject`.
    - Add test dependencies: `uv add --group tests pytest respx`.
    - Initialize `lefthook.yml` with the following content:
      ```yaml
      pre-commit:
        commands:
          uv-lock:
            run: uv lock --check
          ruff-format:
            glob: "*.py"
            run: uv run ruff format {staged_files}
          ruff-check:
            glob: "*.py"
            run: uv run ruff check {staged_files}
          flake8:
            glob: "*.py"
            run: uv run flake8 {staged_files}
          mypy:
            glob: "*.py"
            run: uv run mypy {staged_files}

      pre-push:
        commands:
          pytest:
            run: uv run pytest
      ```
    - Run `lefthook install`.
    - Ensure `.gitignore` excludes `__pycache__` and `state.db`. (venv is outside project root).
    - Configure `ruff` and `flake8` (via `tool.flake8` section thanks to `flake8-pyproject`) in `pyproject.toml`.
  </action>
  <verify>
    <automated>ls -d ~/.virtualenvs/tooling-releases-digest && uv run lefthook status</automated>
  </verify>
  <acceptance_criteria>
    - Virtual environment exists at `~/.virtualenvs/tooling-releases-digest`.
    - `lefthook.yml` is configured with pre-commit (ruff, flake8, mypy) and pre-push (pytest) hooks.
    - `pyproject.toml` contains core dev dependencies including `wemake-python-styleguide`.
    - `respx` and `pytest` are in the `tests` dependency group.
  </acceptance_criteria>
  <done>Project structure and git hooks are initialized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Configuration and Logging</name>
  <files>src/config.py, tests/test_config.py, tests/conftest.py</files>
  <read_first>
    - .planning/phases/01-foundation-and-github-polling/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Settings loads projects from projects.toml correctly.
    - Test 2: Validation fails if GITHUB_TOKEN is missing.
  </behavior>
  <action>
    - Add dependencies: `uv add pydantic-settings structlog httpx`.
    - Create `src/config.py` using `pydantic_settings.BaseSettings`.
    - Include `github_token: str`, `projects_file: Path`, `db_path: Path`, `log_level: str`.
    - Implement `setup_logging()` using `structlog` for JSON output.
    - Create `.env.example` with placeholder `GITHUB_TOKEN`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config.py</automated>
  </verify>
  <acceptance_criteria>
    - `Settings` class is functional.
    - `setup_logging()` is implemented.
    - `httpx` is used as the HTTP client.
  </acceptance_criteria>
  <done>Configuration and logging foundation established.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: State Storage (SQLite with Datetimes)</name>
  <files>src/db.py, tests/test_db.py</files>
  <behavior>
    - Test 1: init_db creates table with TEXT timestamp columns.
    - Test 2: save_release accepts datetime objects and persists them as ISO strings.
    - Test 3: is_release_processed correctly identifies existing records.
  </behavior>
  <action>
    - Implement `src/db.py` using `sqlite3`.
    - Schema: `CREATE TABLE IF NOT EXISTS releases (repo TEXT, tag TEXT, published_at TEXT, discovered_at TEXT, PRIMARY KEY (repo, tag))`.
    - `save_release(db_path: Path, repo: str, tag: str, published_at: datetime)`: Converts `published_at` to ISO format string before insertion. Uses `datetime.now(timezone.utc).isoformat()` for `discovered_at`.
    - `is_release_processed(db_path: Path, repo: str, tag: str) -> bool`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_db.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/db.py` uses `datetime` objects in signatures.
    - SQLite schema uses `TEXT` for timestamps.
    - `uv run mypy src/db.py` passes.
  </acceptance_criteria>
  <done>Database logic handles temporal data correctly using native datetime objects and TEXT storage.</done>
</task>

</tasks>

<verification>
`uv run pytest tests/test_config.py tests/test_db.py` passes successfully.
</verification>

<success_criteria>
- Configuration and logging foundation established
- Local database operations built and tested
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-github-polling/01-01-SUMMARY.md`
</output>